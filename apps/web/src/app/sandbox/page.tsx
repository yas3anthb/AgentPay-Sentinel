import { SandboxView } from "@/components/sandbox/sandbox-view";

export const metadata = { title: "Sandbox · AgentPay Sentinel" };

export default function SandboxPage() {
  return <SandboxView />;
}
