# AgentPay Sentinel Telegram bot (demo only). No third-party Telegram library —
# the Bot API is one HTTPS endpoint with JSON bodies.
httpx==0.28.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pytest==8.3.4
pytest-asyncio==0.25.0
